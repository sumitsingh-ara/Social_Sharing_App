import * as types from "./actionTypes";


const initState ={
    id:null,
    name:null,
    profilePic:null,
    email:null,
}

export const reducer =(state = initState, { type, payload }) =>{
    switch(type){
        
        default:
            return state
    }
}